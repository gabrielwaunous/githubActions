name: "Hola Actions"

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Algo
        run: echo "Hola Actions"
        run: echo "haciendo algo"
        run: echo "sigo haciendo algo"
        run: echo "ya termine"
